# middleware

This package holds Emissary's HTTP middleware — the cross-cutting wrappers that run around the [handlers](../handler/) on every request. It includes CSRF protection, HTTPS redirection, response caching, MIME-type handling, localhost detection, and debugging helpers.

See the [project README](../README.md) for the big picture.
