/*
Package middleware contains HTTP middleware that is specific to Emissary.
These functions wrap regular HTTP handlers, and are used to mix in additional
functionality into multiple handlers.
*/
package middleware
