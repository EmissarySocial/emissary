package model

// DomainColorModeAuto represents color mode that is determined automatically by the client's settings
const DomainColorModeAuto = "AUTO"

// DomainColorModeLight represents light color mode
const DomainColorModeLight = "LIGHT"

// DomainColorModeDark represents dark color mode
const DomainColorModeDark = "DARK"

// DomainMLSModeAll represents MLS mode where all users can use MLS features
const DomainMLSModeAll = "ALL"

// DomainMLSModeGroups represents MLS mode where only users in specific groups can use MLS features
const DomainMLSModeGroups = "GROUPS"

// DomainMLSModeNone represents MLS mode where no users can use MLS features
const DomainMLSModeNone = "NONE"
