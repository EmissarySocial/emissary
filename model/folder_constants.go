package model

// FolderLayoutChat displays a Folder as a chat transcript
const FolderLayoutChat = "CHAT"

// FolderLayoutSocial displays a Folder as a social media timeline
const FolderLayoutSocial = "SOCIAL"

// FolderLayoutNewspaper displays a Folder as a newspaper front page
const FolderLayoutNewspaper = "NEWSPAPER"

// FolderLayoutMagazine displays a Folder as a magazine spread
const FolderLayoutMagazine = "MAGAZINE"

// FolderFilterAll shows every message in a Folder
const FolderFilterAll = "ALL"

// FolderFilterUnread shows only the unread messages in a Folder
const FolderFilterUnread = "UNREAD"
