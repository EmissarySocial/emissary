package vocabulary

import "github.com/benpate/form"

// WYSIWYG renders a WYSIWYG widget in HTML.
func WYSIWYG(library form.Library) {

	/*
		library.Register("wysiwyg", func(f form.Form, s schema.Schema, v interface{}, builder *strings.Builder) error {
			return nil
		}
	*/
}
