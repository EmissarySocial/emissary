version: '3.8'
services:
  emissary:
    build:
      context: .
      dockerfile: ./docker/Dockerfile
    ports:
      - "8080:8080"
    command: ["./emissary", "--config", "file:///data/config/config.json"]
    depends_on:
      - mongodb
      - smtp
    volumes:
      - configdata:/data/config
  setup:
    build:
      context: .
      dockerfile: ./docker/Dockerfile
    ports:
      - "8888:8888"
    command: ["./emissary", "--config", "file:///data/config/config.json", "--port", "8888", "--setup"]
    depends_on:
      - mongodb
      - smtp
    volumes:
      - configdata:/data/config
  mongodb:
    image: "mongo:latest"
    ports:
      - "27017:27017"
    volumes:
      - mongodata:/data/db
  smtp:
    build:
      context: .
      dockerfile: ./docker/Dockerfile-smtpLogger
    ports:
        - "8025:8025"
volumes:
  mongodata:
  configdata:
