# protocols

This directory groups Emissary's implementations of specific federation protocols, each in its own subpackage. Today it holds [websub](websub/), the WebSub publish/subscribe protocol used to receive real-time updates from RSS/Atom feeds. Other protocols may join it here as they are added.

See the [project README](../README.md) for the big picture.
