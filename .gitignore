# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# System files for macOS 
.DS_Store

# Node OMG
node_modules/

# Local Go workspace override -- lets local sibling checkouts (../toot,
# ../toot-echo) take precedence over go.mod's committed replace directives,
# without touching go.mod itself. Machine-specific, never committed.
go.work
go.work.sum

# Application config info
config-env.json
.emissary
/emissary
config.json
.local-native-config.json
/service/geocoder/apikeys_test.go
