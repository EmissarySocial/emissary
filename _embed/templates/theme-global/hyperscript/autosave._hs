behavior Autosave

    on input
        set my isChanged to true
    end

    on blur
        send maybeSave
    end

    on keydown debounced at 10s
        send maybeSave
    end

    on maybeSave
        if my isChanged then 
            set my isChanged to false
            send autosave
        end
    end
end
