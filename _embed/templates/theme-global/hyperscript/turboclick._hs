-- turboclick behavior intentionally disabled.
-- The previous implementation was kept here inside a block comment, but
-- block comments were removed in hyperscript 0.9.90 and now fail to parse.
-- The prior source remains available in git history if it is ever revived.
