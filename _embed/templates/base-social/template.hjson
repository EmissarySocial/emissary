{
	templateId:"base-social"
	actions:{

		follow-button: {
			roles:["authenticated"]
			steps: [
				{do:"view-html"}
			]
		}

		like-button: {
			roles:["authenticated"]
			steps:[
				{do:"set-response"}
				{do:"view-html", "method":"both"}
			]
		}
	}
}