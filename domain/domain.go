/*
Package domain contains the domain-level Factory, along with
the realtime client/server for SSE connections.

The domain.Factory creates, caches, and produces all of the services
that are used by an individual domain.
*/
package domain
