This is a standalone file for PARSING (not decrypting) MLS messages.
It is used to identify the metadata in KeyPackages (for emojikey verification)

It is currently UNUSED, but I'm leaving it here for now because it may be useful at some point in the future.