## honeypot

Honeypot is a simple check on form submissions, to confirm that "honeypot" fields have not been touched.  It does not verify that required fields are populated -- that belongs to schema validation.